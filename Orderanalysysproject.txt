Orderanalysys project3

DT = CALENDARAUTO()

DT1 = CALENDAR(MIN('Orders'[Order Date]),MAX('Orders'[Order Date]))

YEAR = YEAR([Date])

MONTH = MONTH([Date])

MONTH_NAME = FORMAT([Date],"MMM")

DAYID = DAY([Date])

QTR = CONCATENATE("QTR-",FORMAT([Date],"Q"))

CREATE RELATION DT AND ORDERTABLE

CREATE MEASURE TOTAL ORDERS 

1.TOTAL ORDERS = COUNT(Orders[Row ID])

2.TOTAL SALES = SUM(Orders[Sales])

3. TOTAL QTY = SUM(Orders[Quantity])

TOTAL RETURNS ORDER = DISTINCTCOUNT(Returns[Order ID])

TOTAL ORDERS DELIVERED = [TOTAL ORDERS]-[TOTAL RETURNS ORDER]
